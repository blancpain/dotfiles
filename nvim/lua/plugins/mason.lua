-- FIX: temp workaround while breaking changes due to mason v2.0.0 are not resolved
return {
  { "mason-org/mason.nvim", version = "1.11.0" },
  { "mason-org/mason-lspconfig.nvim", version = "1.32.0" },
}
